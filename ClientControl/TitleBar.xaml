<UserControl x:Class="ClientControl.TitleBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ClientControl"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" d:DesignWidth="800" Height="32"
             Background="#6a1b9a" Name="TitleBarUserControl" Loaded="TitleBarUserControl_Loaded">

    <Grid DataContext="{Binding ElementName=TitleBarUserControl}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="auto"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Column="0" Text="{Binding Path=DataContext}" Foreground="White" FontSize="15" VerticalAlignment="Center" Margin="10 0 0 0"/>
        <Border Grid.Column="1" Background="Gray"/>
        <Border Grid.Column="2">
            <Button x:Name="btnClose" Style="{StaticResource MaterialDesignFloatingActionMiniAccentButton}" Width="25" Height="25" Background="{x:Null}" BorderBrush="{x:Null}" Foreground="White" Click="BtnClose_Click">
                <materialDesign:PackIcon Kind="Close"/>
            </Button>
        </Border>
    </Grid>
</UserControl>
